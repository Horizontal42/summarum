node_modules/
dist/
src-tauri/target/
src-tauri/gen/
src-tauri/.cargo/
*.log

# local reference copies of third-party sources — not ours to ship
Contents/
numi-master/

# one-off debugging artifacts
scripts/smoke-*
test_perf.ts

# AI coding agent scratch/notes directories
.jules/

# updater signing key — private, never publish (see .tauri-signing/README.txt)
.tauri-signing/
